@model IEnumerable<AspMonaMvc.Models.CastingModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ImageContent.ImageUrlName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CastHeader)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CastResumeHeader)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ImageContent.ImageUrlName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CastHeader)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CastResumeHeader)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
<div>
    @Html.ActionLink("Back to List", "Index", "Home")
</div>